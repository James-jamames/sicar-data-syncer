services:
  sicar-downloader:
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: uv run python -m src.main
    volumes:
      - /app/.venv
      - ./data:/app/data
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G